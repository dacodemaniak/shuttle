{% extends 'base.html.twig' %}

{% block title %}Shuttle List{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>{{ title }}! ✅</h1>
    
    <ul>
        {%  for tour in tours %}
        	<li>{{ tour.date | date("d-m-Y") }} [<a href="{{ path("shuttle_detail",  {"id": tour.id}) }}">{{ tour.getDate() | date("H:i")}}</a>]</li>
        {%  endfor %}
    </ul>
</div>
{% endblock %}
