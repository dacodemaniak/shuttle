{% extends 'base.html.twig' %}

{% block title %}Shuttle # {{ id }} {% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>{{ title }}! ✅</h1>
    
    <p>Shuttle # {{ id }}</p>
    <p>Places disponibles : {{ shuttle.places }}</p>
    <a role="button" href="{{ path("display_resa_form", {id: id}) }}">Réserver</a>
    <nav>
    	<a href="{{ path("shuttle") }}">
    		Retour à la liste
    	</a>
    </nav>
</div>
{% endblock %}